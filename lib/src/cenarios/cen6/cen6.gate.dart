import 'package:flutter/material.dart';
import 'package:maosunidas/src/cenarios/cen6/interesses.screen.dart';

class Cen6Gate extends StatelessWidget {
  const Cen6Gate({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return InteressesScreen();
  }
}
